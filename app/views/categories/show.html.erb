<h1><%= @category.name %></h1>

<%= image_tag @category.name.delete(" ") + ".jpg" %>

<ul>
	<% @category.soups.each do |soup|%>
		<li><%= link_to soup.name, soup %></li>
	<% end %>
</ul>

<p><%= link_to "Back to Soup Categories", categories_path %></p>